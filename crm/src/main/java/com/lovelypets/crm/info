View  →  Presenter  →  Facade  →  core.jar сервисы
                                        ↓
                               domain.jar (Entity, Enum, Event)

Каждый слой знает только о следующем — это даёт независимость при изменениях в core module и чистую точку входа для тестов на каждом уровне.